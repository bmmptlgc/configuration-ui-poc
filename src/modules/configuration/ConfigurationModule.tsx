import { ReactElement } from 'react';

import ConfigurationRoutes from './ConfigurationRoutes';

const ConfigurationModule = (): ReactElement => {
  return (
    <ConfigurationRoutes/>
  );
};

export default ConfigurationModule;